<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <appender name="GELF" class="de.siegmar.logbackgelf.GelfUdpAppender">
        <graylogHost>logs.empresa.com.br</graylogHost> <!-- Substitua pelo IP/DNS real -->
        <graylogPort>12201</graylogPort>
        <maxChunkSize>508</maxChunkSize>
        <useCompression>true</useCompression>

        <encoder class="de.siegmar.logbackgelf.GelfEncoder">
            <includeRawMessage>false</includeRawMessage>
            <includeMarker>true</includeMarker>
            <includeMdcData>true</includeMdcData>
            <includeCallerData>false</includeCallerData>
            <includeRootCauseData>true</includeRootCauseData> <!-- Recomendado -->
            <includeLevelName>true</includeLevelName> <!-- Útil para visualização no Graylog -->

            <fullPatternLayout class="ch.qos.logback.classic.PatternLayout">
                <Pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</Pattern>
            </fullPatternLayout>

            <staticField>app_name:api-gateway</staticField>
            <staticField>os_name:${os.name}</staticField>
            <staticField>env:${ENVIRONMENT:-dev}</staticField>
        </encoder>
    </appender>

    <!-- <root level="INFO">
        <appender-ref ref="GELF" />
    </root>
    <logger name="com.logging.springboot2graylog.interceptor.RestControllerInterceptor" level="TRACE" additivity="false">
        <appender-ref ref="GELF" />
    </logger> -->
	
	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
        <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
    </appender>
        <root level="ERROR">
    <appender-ref ref="STDOUT" />
    </root>
    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>
